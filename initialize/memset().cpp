#include<bits/stdc++.h>
using namespace std;
const int max_n = 1004;
int a[max_n];
int a2[max_n][max_n];

int main() {
    memset(a, -1, sizeof(a));
    memset(a2, 0, sizeof(a2));
}

/*
void* memset(void *ptr, int value, size_t num);
0 or -1로 배열을 초기화할 때는 memset() 사용
*/